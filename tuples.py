# tuples
# they are similar to list but they are immutable

numbers = (1, 3, 4)
#numbers[0] = 10 this produces error
print(numbers[0])
coordinates = (1, 2, 3)


#unpacking -----> we can use with list as well
x, y, z = coordinates
print(x)
print(y)
print(z)