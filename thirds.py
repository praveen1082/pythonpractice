# define a variable name and set it to jennifer and what is the output if print(name[1:-1])
# without running program i am writting this output will be
# ------------------------ ennife --------------------------------------------------------



name = 'jennifer'
print(name[1:-1])